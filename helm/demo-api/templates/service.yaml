apiVersion: v1
kind: Service
metadata:
  name: demo-api
spec:
  type: ClusterIP
  selector:
    app: demo-api
  ports:
    - port: 80
      targetPort: 3000
# type: LoadBalancer
# ports:
#   - port: 80
#     targetPort: 3000
